package chap00_homework._20230322.Basic;

import java.util.Scanner;

public class _2nd_trytrt {
/*
 2. 사용자가 입력한 문자열에서 가, 바의 개수를 새서 출력하세요.
   0개면 '가, 바가 존재하지 않습니다.'라고 출력.
 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc = new Scanner(System.in);
		System.out.println("가&바 개수를 셀 문자열을 입력해주세요: ");
		String str = sc.nextLine();
	
		

		//총 개수를 세어줄 값도 선언해두자. 없으면 0
		int gaCnt = 0;
		int baCnt = 0;
		
		for(int i = 0; i < str.length(); i++) {
				char cstr = sc.nextLine().charAt(i);
				if(cstr(i) == "가") {
				gaCnt++; 
				System.out.println(gaCnt);}
		}
			
		for(int j = 0; j < str.length(); j++) {
			char cstr = sc.nextLine().charAt(j);
		    if(cstr(j) == "바") {
				baCnt += baCnt;
				System.out.println(baCnt);}	
		  }


		if(gaCnt == 0 && baCnt == 0) {
			System.out.println("가&바가 존재하지 않습니다.");
			
			} else {
				System.out.println("가&바의 개수는 " + (gaCnt+baCnt));
			} 


	}

private static String cstr(int i) {
	// TODO Auto-generated method stub
	return null;
}
			
	}

